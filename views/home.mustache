<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css" />

    <style>
        body {
            padding-top: 50px;
            background-color: #f8f9fa; /* Lighter gray for background consistency */
            font-family: 'Arial', sans-serif;
        }
        .main__container {
            max-width: 800px; /* Restrict maximum width for better layout */
            margin: auto; /* Center the container */
            padding: 30px; /* Add padding around the content */
            background-color: #ffffff; /* White background for the container */
            border-radius: 12px; /* Rounded corners for modern look */
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
            text-align: center; /* Center align the text */
        }
        h2 {
            color: #007bff; /* Consistent heading color */
            margin-bottom: 20px; /* Space below the heading */
        }
        a {
            display: inline-block; /* Block-level for proper padding */
            margin-top: 20px; /* Space above the link */
            padding: 12px 20px; /* Padding around the link */
            background-color: #007bff; /* Button-like background */
            color: white; /* Text color */
            border-radius: 5px; /* Rounded corners for the link */
            text-decoration: none; /* Remove underline */
            transition: background-color 0.3s; /* Smooth hover transition */
        }
        a:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
    </style>

</head>
<body>
    <div class="main__container">
        <h2>Home</h2>
        <a href="/register">Click here to register for the exam</a>
    </div>
</body>
</html>
