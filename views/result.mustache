<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css" />

    <style>
        body {
            padding-top: 50px;
            background-color: #e9ecef; /* Lighter gray background */
            font-family: 'Arial', sans-serif;
        }
        .main__container {
            max-width: 800px;
            margin: auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px; /* Slightly more rounded corners */
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2); /* Stronger shadow for depth */
        }
        h1 {
            text-align: center;
            color: #007bff; /* A bright blue */
            margin-bottom: 20px;
        }
        h2, h3 {
            text-align: center;
            color: #343a40; /* Darker gray for titles */
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #17a2b8; /* Teal for table header */
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f8f9fa; /* Light gray for even rows */
        }
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }
        .button {
            padding: 12px 25px;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold; /* Bold text for buttons */
        }
        .button-primary {
            background-color: #007bff; /* Bright blue */
            transition: background-color 0.3s; /* Transition for hover effect */
        }
        .button-primary:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        .button-success {
            background-color: #28a745; /* Green */
            transition: background-color 0.3s; /* Transition for hover effect */
        }
        .button-success:hover {
            background-color: #218838; /* Darker green on hover */
        }
    </style>

</head>
<body>
    <div class="main__container">
        <h2>Exam Results</h2>
        <h3>Hello, {{ complete_name }} &lt;{{ email }}&gt;</h3>

        <h1>Your Score: {{ total_score }} / {{ question_items }}</h1>

        <table>
            <tr>
                <th>#</th>
                <th>Question</th>
                <th>Choices</th>
                <th>Your Answer</th>
                <th>Correct Answer</th>
            </tr>
            {{#questions}}
            <tr>
                <td>{{ item_number }}</td>
                <td>{{ question }}</td>
                <td>
                    <ul>
                        {{#choices}}
                        <li><strong style="color: #007bff;">{{ letter }}</strong> {{ choice }}</li>
                        {{/choices}}
                    </ul>
                </td>
                <td><strong>{{ user_answer }}</strong></td>
                <td><strong style="color: #28a745;">{{ correct_answer }}</strong></td>
            </tr>
            {{/questions}}
        </table>

        <div class="button-container">
            <a href="/login" class="button button-primary">Restart Exam</a>
            <a href="/examinees" class="button button-success">View All Results</a>
        </div>
    </div>
</body>
</html>
